---
	- name: Gather info about vCenter Extensions
 	  vcenter_extension_info:
  	  hostname: '{{ IDM-CLIENT }}'
   	  username: '{{ Konsalt\bogachan.bilgin }}'
  	  password: '{{ A.b15aysun }}'
  	  register: ext_info
  	  delegate_to:
